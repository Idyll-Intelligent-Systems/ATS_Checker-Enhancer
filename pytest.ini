[pytest]
addopts = --cov=src/core --cov=src/ai/inhouse --cov=src/dashboard --cov-branch --cov-report=term-missing:skip-covered --color=yes
# Start with a moderate threshold; enforce via .coveragerc fail_under
